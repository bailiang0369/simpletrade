# 事件合约 H=15 方向预测 · 实验日志

目标：ETH/BTC，无前视约束，≥65% 准确率，≥14 tpd（每天交易笔数）。

训练段 ≤ 2024-06-30；es 2024-06-30→2024-09-30；meta_val 2024-09-30→2025-09-30；test 2025-09-30→2026-08-29。

## 术语
- tp d = trades per day, 每日下注次数
- q = 置信度分位数阈值（滚动 30 天历史分布）
- ε = 收益阈值（过滤微幅振荡，|ret|>ε 才下注）
- AUC = ROC-AUC 全样本
- Acc = 选中样本的方向命中率（方向正确/总选）

---

## ✗ 已试失败（2026-09-22）

| # | 尝试 | es/test AUC | 无前视 test (q=99) | 失败结论 |
|---|------|------------|-------------------|---------|
| 1 | LGB 单模型 seed42 硬分类二值标签 | 0.5349 / — | 59.30% @ 13.3t | — |
| 2 | LGB 10 seed 排名集成 | — / 0.5426 | 63.56% @ 14.3t | **最好基线** |
| 3 | 手工特征加 Stochastic/RSI/MACD (17新特征) | AUC 不升反降 | — | 噪声，纯特征工程挤不出新信息 |
| 4 | XGB 单币种训练 + 10 seed | — / — | 几乎与 LGB 相同（高度相关） | 三族树模型高度共线，集成增益极小 |
| 5 | CatBoost 10 seed | — / — | 同 LGB（0.533 AUC） | 同上 |
| 6 | 30模型 LGB+XGB+CAT 排名集成 | — / 0.543 | 63.56% @ 14.3t vs LGB 10seed 63.22% | **集成边际增益 <0.3pp**，树族已榨干 |
| 7 | GRU 6通道原始序列 (lr1/imb/rvol/stoch + BTC lr1/imb) | 0.538 / 0.5316 | 55.8% @ 13.9t | **GRU 严重过拟合**（es AUC 高于 test）；原始序列信息量远弱于手工特征；GRU+LGB 集成反降 |
| 8 | soft_label 回归 (LGB regression) 用硬 AUC 指标 | 0.5199 | — | 指标锁死分类天花板，正确做法是 Spearman/IC |

### 经验（从失败中提炼）

1. **硬分类 AUC 天花板 ~0.534**：当前 58 特征 + LGB，无论怎么堆 seed/模型族，AUC 稳在 0.534-0.543 区间，树结构表达力已到顶。
2. **集成边际递减**：LGB 10seed → +2pp；LGB+XGB 20seed → 0.0pp；+CAT 30seed → +0.3pp。相关性太高。
3. **原始序列（GRU/CNN）不自动优于手工特征**：除非网络足够深/足够数据，CPU 上 23.5万样本/降采样过轻，GRU 反而过拟合。
4. **回归目标不能用 AUC 评估**：要用 Spearman 排名相关 / IC，再通过单调性映射回概率。
5. **微幅振荡是标签噪声**：close[t+15] > close[t] 含大量 ret=0.001% 的样本，其方向标签本身不可预测，必须过滤。
6. **阈值必须在 meta_val 选**：不能反复扫 test。

## ✓ 已验证有效

| 方法 | 提升 | 说明 |
|------|------|------|
| 多 seed 排名集成 (LGB) | AUC +0.008，acc +2pp | 跨 seed 降方差最有效，树间无严重相关 |
| 多空独立选方向 | acc +2pp | 模型对多/空头的置信度分布不对称，分开取阈值更准 |
| 滚动 30 天分位阈值 | 无前视 | trade-off 稳 |

---

## 🔬 当前尝试（2026-09-22 下午）

### 线 A：过滤微幅振荡 + 回归 LGB + 重新打分

**假设**：
- |ret| < ε 的样本，标签本身是噪声（振荡 + 舍入误差），模型学不到东西
- 用回归目标直接学 ret（或 soft_label），评估用 Spearman/IC，最终单调性映射

**方法**：
1. 重新生成标签：保留全样本用于训练，但推理时**只在 |ret|（历史估计）高于阈值 ε 的时点下注**
2. 训练：LGB 回归 soft_label（ret 压缩到 [0,1]），评估 Spearman on es
3. 阈值搜索：meta_val 扫 ε ∈ {0, 0.0003, 0.0005, 0.001, 0.0015} + q ∈ {98, 98.5, 99, 99.2, 99.5}
4. 锁死 (ε*, q*) 后 test 只跑一次

**状态**：进行中

---

### 线 B：阈值正规化

**之前问题**：每次评估都在 test 扫 q 值，隐式前视。

**修正**：
1. 在 meta_val 网格搜 (ε, q) 找 (65% acc, ≥14 tpd) 最优组合
2. 锁死参数后 test 做**一次且仅一次**无前视评估

**状态**：待线 A 完成后衔接

---

### 线 C：spearman-regression LGB

**假设**：用 soft_label 做回归目标（不是硬分类），LGB 的 `regression_objective` / `poisson` 训练后，预测值的排序质量（Spearman）应该优于 hard binary AUC。

**方法**：
1. soft_label = 0.5 + 0.5 * clip(ret_future / 0.02, -1, 1)（把未来收益归一到 [-2%, 2%] → [0,1]）
2. 训练：LGB `objective=regression`，eval_metric=Spearman / 直接用自定义 AUC on hard label 做 early_stop
3. 预测出来的 yhat 是软分数，直接用于排名（不用 sigmoid，因为训练目标不是概率）

**状态**：待跑

---

## 待试（如果 A/B/C 仍未达 65%）

### 线 D：BTC 也训 GRU + 跨资产 GRU→ETH 融合
- 已训的 ETH GRU AUC 0.531，BTC 没训
- 跨资产特征（BTC lr 窗口）在 ETH 模型里已有，跨模型融合可能抬升

### 线 E：不同 horizon 对比 (H=5/30/60)
- 看看是不是 H=15 本身最难，换个 horizon 可能 AUC 更高
- 如果 H=30 无前视能到 65%，再考虑 event contract 的灵活性

### 线 F：两币合并"同时强信号才下注"
- ETH top信号 ∩ BTC top信号 → 牺牲 tpd 换准确率
- 预期准确率抬升，tpd 降低

---

## 关键脚本路径

| 脚本 | 功能 |
|------|------|
| `backtest_short/train_lgb_lean.py` | 单币种单seed LGB 训练 + P 保存 |
| `backtest_short/train_xgb_lean.py` | 单币种单seed XGB |
| `backtest_short/train_cat_lean.py` | 单币种单seed CAT |
| `backtest_short/eval_nolookahead.py` | 无前视评估（多模型族） |
| `backtest_short/eval_gru.py` | GRU 专用无前视评估 |
| `backtest_short/params_gru.py` | GRU 训练脚本 |
| `backtest_short/probe_stat.py` | 单变量统计探底 |
| `features.py` | 58 维特征生成 |
| `data_store.py` | 时间切分 + 标签 + AssetContext |
| `config.py` | DS_DIR = data/datasets/ |
| `experiments/experiment_log.md` | **本文件** |

---

## 🔬 后续尝试（2026-09-22 下午）

### 月度稳定性分析 → 发现 regime 依赖

| 月份 | ETH acc | tpd | 备注 |
|------|---------|-----|------|
| 2025-11 | 57.8% | 18.6 | 差 |
| 2025-12 | **74.6%** | 10.5 | 好 |
| 2026-01 | 63.8% | 18.2 | 中等 |
| 2026-02 | 57.8% | 16.9 | 差 |
| 2026-03 | 63.4% | 16.7 | 中等 |
| 2026-04 | **78.2%** | 14.2 | 好 |
| 2026-05 | **53.6%** | 20.8 | 极差 |
| 2026-06 | 67.1% | 12.8 | 好 |
| 2026-07 | 58.9% | 12.8 | 差 |
| 2026-08 | 66.4% | 15.7 | 好 |

**月度 acc 标准差 7.8pp** → 模型在震荡月失效、单边月好用。说明信号强度与市场 regime 高度相关。

### Gate 过滤测试（只在高信号 regime 下注）

**meta_val 切片发现**：
| 特征切片 | acc | n |
|---------|-----|---|
| rvol_30 Q4 (top 25% 波动) | **65.4%** | 497 |
| hour_sin Q4 (美盘) | **65.5%** | 678 |
| mom_60 均匀 | 61%（无差异）| — |

**完整 gate 测试结果（无前视、锁参数）**：

| 组合 | meta_val acc | meta_val tpd | test acc | test tpd | 结论 |
|------|-------------|-------------|---------|---------|------|
| ETH qc=99 无 gate | 61.23% | 14.2 | **63.22%** | **14.2** | 基线，达标 tpd 但 acc 差 2pp |
| ETH qc=99.5 + hour gate 60 | 64.2% | 7.4 | 64.9% | 7.1 | acc 接近 65 但 tpd 不够 |
| ETH qc=99.2 + rvol gate 75 | 64.7% | 3.5 | 65.3% | 3.1 | acc 够但 tpd 远不够 |
| BTC qc=99 + hour gate 60 | 61.65% | 7.0 | 62.04% | 6.8 | BTC 天生比 ETH 差 |
| ETH+BTC 两币合并 | — | — | **62.84%** | **21.0** | 合计 acc 仍未到 65% |

### 自适应阈值（在线反馈）

**假设**：近 N 天模型表现差就收紧 q，好就放松 q。
**结果**：meta_val ETH 最优 qb=98.5 qt=99.5 ql=97.5 lb=10 → acc=58.9%（反而下降）。
**失败原因**：近 N 天样本量太少（<200 笔），acc 估计噪声大，阈值抖动引入额外噪声。

### soft_label 回归 LGB

**假设**：回归目标比硬分类能利用更多连续信息。
**结果**：soft_label regression LGB hard AUC=0.512（比 binary 0.534 差 0.02）。
**失败原因**：H=15 方向本质是硬分类任务，回归会摊平标签并惩罚"极端正确"样本。

### 最终结论

**ETH H=15 事件合约方向预测，无前视约束下的真实可交易天花板**：
- **最好水平（ETH test q=99 无前视）：acc=63.22% tpd=14.2**
- **月度标准差 7.8pp**（稳定稳定性差）
- **BTC 天生更难**：acc 低 1-3pp

**核心瓶颈**：AUC 天花板 0.534（硬分类二值标签 + 58 维手工特征 + 树模型已榨干），无论换模型族、换回归目标、加 gate，都无法突破这个 AUC→acc 转化上限。

---

## ✅ 已验证有效

| 方法 | 效果 | 适用 |
|------|------|------|
| 多 seed 排名集成 (LGB × 10) | AUC +0.008，acc +2pp | **首要保留** |
| 每方向独立历史滚动分位数阈值 | 无前视 | **首要保留** |
| 降采样 1/5 / batch512 训练 | 让 GRU 跑起来 | CPU 训练 |
| torch CPU 单 batch 22ms | 速度基准 | GRU 单样本推理 |

---

## ❌ 没用/反效果 (全量列表)

| # | 尝试 | 代价 | 结果 |
|---|------|------|------|
| 1 | 加 17 个新技术指标 (Stoch/RSI/MACD) | 1 min build | AUC 从 0.5349 → 0.5332 **下降** |
| 2 | XGB 单模型 + 10 seed | 3 min/seed | AUC 0.534，与 LGB 集成后 **增益 <0.3pp** |
| 3 | CatBoost 单模型 + 10 seed | 2 min/seed | AUC 0.533，与 LGB+XGB 集成后 **几乎不变** |
| 4 | soft_label 回归 LGB | 5 min 训练 | AUC 0.512，**比 hard binary 差 0.02** |
| 5 | GRU 6通道原始序列 | 35 min 训练 + 2 min 推理 | best_auc=0.538（es）但 test 只有 0.5316，**严重过拟合**。无前视 acc 55-57%，比 LGB 差 |
| 6 | rvol 5min gate（先过滤高波动再走 conf 阈值） | 2 min | meta_val acc +1pp 但 tpd 从 14 → 3 |
| 7 | hour gate（美盘下注） | 1 min | acc +1pp 但 tpd 从 14 → 7 |
| 8 | 双 gate (hour + rvol) | 2 min | 单 gate 已足够，双 gate tpd 更低 |
| 9 | 在线自适应阈值（近 10 天表现反馈） | 3 min | acc **从 61% → 59%**，引入噪声 |
| 10 | 30 模型全族集成 | 15 min 训 + 1 min eval | AUC **0.543**（仅比 10 seed LGB +0.001） |

---

## 🚀 未来方向建议（如果要继续冲 65%）

按优先级排序：

1. **[最高ROI] 换 horizon 尝试**：H=5 分钟（更短，可能方向信号更强）、H=30 分钟（更长，可过滤噪声）。这是最快出结果的——换 `config.HORIZON=5/30` 重训即可。
2. **[高难度] 跨 horizon stacking**：同时训练 H=5/15/30 三个模型，meta-learner 用 3 个 P 做集成。
3. **[改口径] 接受 trade-off**：ETH q=99 无 gate tpd=14.2 → meta_val 调 q 到 99.2，tpd=11.6，test acc 有望 64-65%。
4. **[换资产] 放弃 BTC**：BTC 天生 harder（基础波动率低），ETH 单独做可能够 65%。
5. **[换数据] 加非技术特征**：订单簿深度、资金费率、OIA/多空比——如果能弄到。
6. **[深度学习] GRU + 更强正则 + 更多通道**（需要 GPU）。
